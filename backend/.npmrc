# npm 11 blocks install-time lifecycle scripts by default (allow-scripts policy).
# esbuild runs a postinstall to select its platform binary; without this the
# `npm ci` in Render's build hard-fails. npm 10 (local) ignores this key just
# fine, so one file covers both.
allow-scripts=esbuild